<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
           http://www.liquibase.org/xml/ns/dbchangelog
           http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="create-password-reset-token-table" author="system">

        <createTable tableName="password_reset_token">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints nullable="false" primaryKey="true"/>
            </column>

            <column name="code" type="VARCHAR(120)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="user_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="expiresAt" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="used" type="BOOLEAN">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="password_reset_token"
                baseColumnNames="user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                constraintName="fk_pwdreset_user"
                onDelete="CASCADE"/>

        <createIndex indexName="ix_pwdreset_code"
                     tableName="password_reset_token"
                     unique="true">
            <column name="code"/>
        </createIndex>

        <createIndex indexName="ix_pwdreset_user_id"
                     tableName="password_reset_token">
            <column name="user_id"/>
        </createIndex>

        <createIndex indexName="ix_pwdreset_expires_at"
                     tableName="password_reset_token">
            <column name="expiresAt"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
